{
	"name": "bais",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "BAIS — Basically A made-up Issue Standard (BAML). One Issue = one file in .bais/issues/, git is the hosting.",
	"bin": {
		"bais": "./dist/src/cli.js"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "node scripts/lease-race.mjs && node scripts/sync-test.mjs && node scripts/mcp-test.mjs && node scripts/reducer-determinism.mjs && node scripts/cross-check.mjs ../bi/.bais/issues && node scripts/ingest-durability.mjs && node scripts/move-unblocked.mjs && node scripts/ready-wait.mjs && node scripts/content-ids.mjs"
	},
	"dependencies": {
		"@boundaryml/baml-bridge": "0.17.0"
	},
	"devDependencies": {
		"@types/node": "22.19.19",
		"typescript": "5.9.3"
	}
}
