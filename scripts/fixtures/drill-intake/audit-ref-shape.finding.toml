id = "audit-ref-shape"
title = "Malformed audit refs fail bare, never resolve"
source = "audit 2026-09-06: parseAuditRefs (audit.mjs:91-103) silently drops refs with n=0 / non-integer n / missing # — ref-parse arm pins valid shapes only"
property = "a close citing Evidence: audit(t#a1#0) or audit(t#a1#x) resolves to zero refs and fails AUDIT_REQUIRED (bare), never to a resolvable or half-matching ref"
suite = "audit"
arm = "ref-malformed-bare"
status = "candidate"
fixture = "auditCloseProblems over the t#a1 anchor with bodies carrying audit(t#a1#0), audit(t#a1#x), audit(no-hash-here)"
exact_failure = "AUDIT_REQUIRED for t#a1 on each malformed body (refs parsed: 0)"
